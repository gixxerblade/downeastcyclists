// Authentication state should be checked using `getCurrentUser` from `src/utils/firebase.ts` for client-side operations.
// For server-side operations, ID tokens should be verified in middleware or API routes.
