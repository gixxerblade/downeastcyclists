rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All membership data has been migrated to Postgres.
    // Only trails remain in Firestore, accessed via Admin SDK (bypasses rules).
    match /{document=**} {
      allow read: if false;
      allow write: if false;
    }
  }
}
